<div class="card mx-auto mb-5" style="width: 70rem;">
  <div class="card-header">
    <%= @post.user.name %>
  </div>
  <div class="card-body">
    <%= render "carousel", post: @post %>
    <div class="card-text">
      <%= @post.caption %>
    </div>
    <hr>
    <%= link_to 'New Comment', new_post_comment_path(@post), class: 'btn btn-success' %>
    <div class="like_buttons_<%= @post.id %>">
      <%= render "likes/like", object: @post %>
    </div>
    <p id="likes_count_<%= @post.id %>"> Likes: <%= @post.likes.count %> </p>
  </div>
  <div class="card-footer">
    <% if @post.user == current_user %>
      <%= link_to 'Edit', edit_post_path(@post) %> |
      <%= link_to 'Destroy', post_path(@post),
            method: :delete,
            data: { confirm: 'Are you sure?' } %> |
      <%= link_to 'Back', user_path(current_user) %>
    <% else %>
      <%= link_to 'Back', posts_path %>
    <% end %>
  </div>
  <div class="card-footer">
    <%= render @post.comments %> <br>
  </div>
</div>
<br>
